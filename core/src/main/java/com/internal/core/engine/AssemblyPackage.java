package com.internal.core.engine;

public class AssemblyPackage extends ManagerPackage {

    /*
     * Base class for assemblies — groupings of managers, systems, or pipelines
     * that have no particular relationship to each other beyond always being
     * created together. Unlike PipelinePackage, an assembly implies no single
     * purpose or data flow between its members.
     */
}
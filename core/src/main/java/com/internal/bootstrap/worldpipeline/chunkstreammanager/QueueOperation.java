package com.internal.bootstrap.worldpipeline.chunkstreammanager;

public enum QueueOperation {
    LOAD,
    ASSESSMENT,
    BUILD,
    MERGE,
    ITEM_LOAD,
    ITEM_RENDER,
    BATCH,
    RENDER,
    DUMP,
    SKIP;
}
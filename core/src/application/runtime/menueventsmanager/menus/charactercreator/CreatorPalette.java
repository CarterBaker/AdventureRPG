package application.runtime.menueventsmanager.menus.charactercreator;

public enum CreatorPalette {

    /*
     * The color palettes the creator paints swatches from. A swatch's click
     * argument is its palette name and its index in that palette.
     */

    SKIN,
    HAIR
}

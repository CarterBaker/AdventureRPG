package application.bootstrap.menupipeline.element;

public enum ElementType {

    /*
     * Classifies the visual and interactive role of a UI element. Determines
     * how the element is rendered and whether it participates in hit testing.
     */

    SPRITE,
    TEXTURE,
    BUTTON,
    LABEL,
    CONTAINER,
    TOOLBAR,
    EXPANDABLE_CONTAINER;
}
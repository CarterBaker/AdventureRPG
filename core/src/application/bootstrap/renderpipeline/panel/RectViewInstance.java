package application.bootstrap.renderpipeline.panel;

public class RectViewInstance extends PanelViewInstance {

    @Override
    public void render(int x, int y, int w, int h) {
        // UI content rendering hooks in here. Scissor is applied by the caller.
    }
}
